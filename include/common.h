#ifndef COMMON_H
#define COMMON_H

#define NULL 0

#define ABS(x) ((x)<0 ? -(x) : (x))
#define ABSF(x) ((x)<0.0f ? -(x) : (x))

#include "types.h"
#include "macros.h"
#include "include_asm.h"

#include "structs.h"
#include "functions.h"
#include "variables.h"

#endif
